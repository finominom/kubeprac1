apiVersion: v1
kind: PersistentVolume
metadata:
  name: mypv
  labels: 
    app: myapp
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Recycle
  storageClassName: slow
 
